# 2026-03-19 00:21:06 KST

## 핵심 로그

- `nwm:cu126` 이미지 확인 후 `nwm_dev` 컨테이너를 detached 모드로 실행했고, `scripts/docker/nwm-run.sh`로 컨테이너 내부 명령 실행 흐름을 정리함.
- RECON raw `.hdf5` 데이터셋 로딩을 실제로 검증했고, `EvalDataset(recon)` 기준 shape와 샘플 인덱스 로딩이 정상 동작함을 확인함.
- XL 체크포인트 + 로컬 VAE로 1-sample forward를 돌려 RECON inference smoke test를 통과시킴.
- `torchaudio` ABI mismatch를 해결하기 위해 컨테이너 내부 패키지를 `torch==2.10.0`, `torchvision==0.25.0`, `torchaudio==2.10.0` 조합으로 맞춤.
- `planning_eval.py` import 실패 원인을 `torch` import 이후 잘못 잡히는 `libstdc++` 링크 순서로 좁혔고, `Dockerfile`과 `scripts/docker/nwm-run.sh`에서 `CONDA_PREFIX` / `LD_LIBRARY_PATH`를 명시해 해결함.
- `scripts/recon/recon_smoke_test.py`를 추가해서 RECON 데이터셋 로딩과 1-sample forward를 바로 재현할 수 있게 함.
- 스모크 테스트 출력 이미지는 프로젝트 내부 `artifacts/recon_smoke`에 저장되도록 바꿈.
- `scripts/docker/nwm-start.sh`를 추가해서 컨테이너 생성/시작을 분리했고, `NWM_GPU_REQUEST=all` 또는 `NWM_GPU_REQUEST='device=0,1'`로 GPU 가시성을 설정할 수 있게 함.
- 실제로 `docker rm -f nwm_dev`, `NWM_GPU_REQUEST='device=0,1' ./scripts/docker/nwm-start.sh`, `./scripts/docker/nwm-run.sh "python -c 'import torch; print(torch.cuda.device_count())'"` 흐름을 실행해 GPU 2개가 보이는 것을 확인함.

## 관련 커밋

- `da1656a` Align container runtime env and torch stack
- `37ba705` Add smoke test and configurable GPU container startup
- `54418d3` Fix quoted Docker device GPU requests
