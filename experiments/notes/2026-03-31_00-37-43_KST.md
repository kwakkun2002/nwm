# 2026-03-31 00:37:43 KST

## 핵심 로그

- 세 체크포인트 세트에 대해 `RECON time` checkpoint sweep를 완료함.
  - `weights/checkpoints/nwm_cdit_s_recon_128`
  - `weights/checkpoints/nwm_cdit_s_recon_128_text_dense`
  - `weights/checkpoints/nwm_cdit_s_recon_raw_text_dense`
- 평가 실행 전 `nwm_dev` 컨테이너가 내려가 있는 상태를 확인했고, `8g` shared memory 설정이 잡힌 상태로 재사용함.
- `scripts/eval/run_time_checkpoint_sweep.py`를 권장 옵션으로 실행해 전체 sweep를 완료함.
  - `--infer_batch_size 16`
  - `--eval_batch_size 64`
  - `--num_workers 4`
  - `--skip_existing 1`
  - `--include_latest 1`
- sweep 결과 `artifacts/time_checkpoint_sweep_manifest.json`을 재생성함.
  - 총 `26` 엔트리
  - 기존 결과 재사용 `5`
  - 새로 실행 `21`
- 실험별 최종 `recon_time.json` 개수 확인:
  - `artifacts/eval_s_recon_128`: `12`
  - `artifacts/eval_s_recon_128_text_dense`: `7`
  - `artifacts/eval_s_recon_raw_text_dense`: `7`
- plotting 과정에서 `artifacts/eval_s_recon_128/b64/.../recon_time.json` 같은 nested legacy 결과가 재귀 스캔에 섞여 들어가는 문제를 확인함.
- `scripts/presentation/plot_recon_time_over_checkpoints.py`를 수정해 direct run 디렉터리만 읽도록 바꿨고, 같은 step의 `latest` alias와 숫자 체크포인트가 함께 있을 때는 숫자 체크포인트를 우선하도록 정리함.
- 같은 plotting 스크립트에서 x축 tick label을 `35`도 기울여 보이도록 조정함.
- 초기 그래프에서 `nwm_cdit_s_recon_128/latest`가 `57.5k`에서 급락하는 이상치처럼 보였음.
- 확인 결과 `artifacts/eval_s_recon_128/nwm_cdit_s_recon_128_latest/recon_time.json`은 현재 `latest.pth.tar`보다 오래된 산출물이라 stale artifact였음.
  - stale artifact `recon_time.json` 수정 시각: `2026-03-26 01:25:25 +0900`
  - 현재 `weights/checkpoints/nwm_cdit_s_recon_128/latest.pth.tar` 수정 시각: `2026-03-27 07:38:12 +0900`
- stale `latest` 결과 디렉터리를 컨테이너 내부에서 삭제한 뒤, 현재 `latest.pth.tar(train_steps=57500)`를 `time` 기준으로 다시 inference/eval함.
- 재평가 결과 `57.5k`는 회귀가 아니라 정상 포인트로 확인됨.
  - `55k` 평균: `LPIPS 0.2977`, `DreamSim 0.1941`, `FID 32.7771`
  - `57.5k` 재평가 평균: `LPIPS 0.2961`, `DreamSim 0.1917`, `FID 32.7505`
- 재평가 후 CSV/PNG를 다시 생성했고, 기존의 `57.5k 급락` 해석은 폐기함.

## 최종 해석

- `128 No Text`는 `5k -> 55k/57.5k`까지 가장 강하게 개선됨.
- `128 No Text`의 최상위 구간은 `55k`와 `57.5k`이며, 재평가된 `57.5k`가 미세하게 더 좋음.
- `128 Text Dense`는 `30k`까지 완만하고 안정적으로 개선됨.
- `Raw Text Dense`는 `5k -> 30k` 동안 metric 변화폭이 매우 작아 거의 평평한 추세를 보임.
- 따라서 이전에 보였던 `57.5k` 급락은 checkpoint 품질 문제가 아니라 stale `latest` evaluation artifact 재사용 때문에 생긴 오염이었음.

## 관련 산출물

- `artifacts/time_checkpoint_sweep_manifest.json`
- `artifacts/eval_s_recon_128/nwm_cdit_s_recon_128_0055000/recon_time.json`
- `artifacts/eval_s_recon_128/nwm_cdit_s_recon_128_latest/recon_time.json`
- `artifacts/eval_s_recon_128_text_dense/nwm_cdit_s_recon_128_text_dense_latest/recon_time.json`
- `artifacts/eval_s_recon_raw_text_dense/nwm_cdit_s_recon_raw_text_dense_latest/recon_time.json`
- `gpu_plots/recon_time_over_checkpoints/01_recon_time_over_checkpoints.csv`
- `gpu_plots/recon_time_over_checkpoints/01_recon_time_over_checkpoints.png`

## 관련 파일 변경

- `scripts/presentation/plot_recon_time_over_checkpoints.py`
