# 2026-03-22 23:46:12 KST

## 학습 종료 상태 확인

- `config/nwm_cdit_s_recon_raw_text_dense.yaml` 학습은 `30k`까지만 보고 멈추기로 했던 상태를 확인함.
- 현재 train 프로세스와 watcher 프로세스는 모두 종료됨.
- host `nvidia-smi` 기준 GPU는 idle 상태로 돌아옴.

## 30k 결과

- 마지막 의미 있는 학습 목표 지점:
  - `step 30000`
- 해당 시점 로그:
  - `loss 0.1212`
  - `Train Steps/Sec 2.13`
  - `Samples/Sec 136.53`
- 직후 저장:
  - `weights/checkpoints/nwm_cdit_s_recon_raw_text_dense/0030000.pth.tar`
  - `weights/checkpoints/nwm_cdit_s_recon_raw_text_dense/latest.pth.tar`
- 로그상 저장 직후 `30010`, `30020`, `30030`이 잠깐 더 찍혔지만, 실사용 기준 checkpoint cutoff는 `30k`로 간주함.

## 확보된 산출물

- log:
  - `logs/nwm_cdit_s_recon_raw_text_dense/log.txt`
- stdout:
  - `logs/nwm_cdit_s_recon_raw_text_dense/stdout_live_20260322_193908.log`
- checkpoints:
  - `weights/checkpoints/nwm_cdit_s_recon_raw_text_dense/0005000.pth.tar`
  - `weights/checkpoints/nwm_cdit_s_recon_raw_text_dense/0010000.pth.tar`
  - `weights/checkpoints/nwm_cdit_s_recon_raw_text_dense/0015000.pth.tar`
  - `weights/checkpoints/nwm_cdit_s_recon_raw_text_dense/0020000.pth.tar`
  - `weights/checkpoints/nwm_cdit_s_recon_raw_text_dense/0025000.pth.tar`
  - `weights/checkpoints/nwm_cdit_s_recon_raw_text_dense/0030000.pth.tar`
  - `weights/checkpoints/nwm_cdit_s_recon_raw_text_dense/latest.pth.tar`

## 현재 판단

- `S + raw RECON + dense text cache` 조합은 최소 `30k`까지 안정적으로 학습됨.
- 다음 스케일은 그대로 `B`로 진행하는 게 맞음.
- 비교 기준은 이미 준비한 `config/nwm_cdit_b_recon_raw_text_dense.yaml`를 사용.

## 다음 액션

- `0030000.pth.tar` 기준으로 샘플 inference / eval을 붙일지 결정
- `B` 동형 config로 짧은 안정성 run 시작
