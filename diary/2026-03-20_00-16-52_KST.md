# 2026-03-20 00:16:52 KST

## 오늘 추가로 한 일

- `scripts/build_caption_manifest.py` 추가:
  - raw RECON `.hdf5` 기준으로 `trajectory_name + frame_time` manifest 생성 가능하게 함.
  - `--frame-stride`, `--limit-trajs`, `--max-frames-per-traj` 지원.
- `scripts/export_recon_1fps_dataset.py` 추가:
  - raw RECON `.hdf5`를 downsampled processed dataset 구조로 export 가능하게 함.
  - 출력 구조는 `traj/0.jpg ... traj_data.pkl`
  - `frame_stride=10` 기준으로 raw 10 Hz -> 1 fps에 대응
- `scripts/generate_qwen_captions.py` 확장:
  - 기존 `image_path` 기반 입력 외에
  - `--data-root + trajectory_name + frame_time` 조합으로 raw RECON 프레임을 직접 읽을 수 있게 함.
- `text_pipeline.py` 정제 규칙 보강:
  - `the scene shows/depicts/features/contains` boilerplate 제거 추가.

## 실데이터 smoke 실행 결과

- 컨테이너를 `/home/wklee/models` mount 포함해서 재생성:
  - `NWM_HOST_MODELS_DIR=/home/wklee/models ./scripts/nwm-start.sh`
- Qwen 경로 확인:
  - `/workspace/nwm/weights/pretrained/Qwen2-VL-7B-Instruct`
- subset manifest 생성:
  - 데이터 루트: `/workspace/nwm/datasets/recon_raw/recon_release`
  - split: `/workspace/nwm/data_splits/recon/test/traj_names.txt`
  - 샘플링: `2 traj x 4 frames`, `frame_stride=10`
  - 출력: `/workspace/nwm/datasets/derived/phase1_recon_subset/manifest.jsonl`
- Qwen caption smoke 실행 완료:
  - 출력: `/workspace/nwm/datasets/derived/phase1_recon_subset/captions_raw.jsonl`
  - 실제 raw caption 8개 생성 확인
- caption cleaning 재실행 완료:
  - 출력: `/workspace/nwm/datasets/derived/phase1_recon_subset/captions_clean.jsonl`
  - boilerplate 제거 후 짧은 sentence 형태로 정제 확인
- CLIP embedding precompute 완료:
  - 모델: `openai/clip-vit-base-patch32`
  - 출력: `/workspace/nwm/datasets/derived/phase1_recon_subset/text_embeds`
  - trajectory별 `.npz` 2개 생성 확인
  - shape:
    - `jackal_2019-08-08-17-49-25_1_r00.npz`: `(4, 512)`
    - `jackal_2020-01-04-13-06-12_5_r00.npz`: `(4, 512)`

## dataloader / model smoke

- raw RECON 한 trajectory로 작은 smoke split 생성:
  - `/workspace/nwm/datasets/derived/phase1_recon_subset/smoke_split/traj_names.txt`
- `TrainingDataset`를 text cache 포함해서 실제 로드 확인:
  - `dataset_len = 11`
  - `obs_shape = (2, 3, 224, 224)`
  - `goal_pos_shape = (1, 3)`
  - `text_emb_shape = (1, 512)`
  - `text_emb_nonzero = True`
- fresh `CDiT-S/2` 모델에 random latent + cached text embedding을 넣어 forward 확인:
  - `model_out_shape = (1, 8, 28, 28)`

## 정렬 문제 대응

- subset smoke를 돌려보니 raw RECON은 약 10 Hz라서
  - raw 시간축 그대로 학습하면서 1 FPS text cache만 쓰면
  - exact match가 안 맞아 zero fallback이 많아질 수 있다는 점이 더 명확해졌음.
- 대응으로 `scripts/export_recon_1fps_dataset.py`를 추가했고, 실제로 1 trajectory export smoke를 돌림:
  - 입력: `/workspace/nwm/datasets/recon_raw/recon_release`
  - 출력: `/workspace/nwm/datasets/derived/phase1_recon_subset/recon_1fps_export`
  - 결과: `jackal_2019-08-08-17-49-25_1_r00`에서 `11` frames export 성공
  - downsampled `traj_data.pkl` shape 확인:
    - `position`: `(11, 2)`
    - `yaw`: `(11,)`

## 현재 판단

- Phase 1의 "구현"은 거의 정리됨.
- raw RECON 기준 subset end-to-end smoke는 통과했다고 봐도 됨.
- 아직 안 끝난 건 full dataset용 1fps export, full caption run, full embedding run, 최종 aligned dataset 산출물 생성임.

## split-level 진행

- RECON 전체 규모를 계산함:
  - train: `9468 traj`, raw `489991` frames, stride10 기준 `53395` frames
  - test: `2367 traj`, raw `120920` frames, stride10 기준 `13193` frames
- 디스크 여유 확인:
  - `/home/kun/kun_ssd/nwm` 기준 약 `3.0T` 여유
- RECON test split full export 완료:
  - 출력: `/workspace/nwm/datasets/recon_1fps_test`
  - 결과: `2367 trajectories`, `13193 frames`
- exported RECON test split full manifest 생성 완료:
  - 출력: `/workspace/nwm/artifacts/phase1/recon_test_1fps_manifest.jsonl`
  - 결과: `13193 records`
- RECON train split full export 완료:
  - 출력: `/workspace/nwm/datasets/recon_1fps_train`
  - 결과: `9468 trajectories`, `53395 frames`
- exported RECON train split full manifest 생성 완료:
  - 출력: `/workspace/nwm/artifacts/phase1/recon_train_1fps_manifest.jsonl`
  - 결과: `53395 records`

## 다음으로 바로 할 일

1. Qwen full caption run
   - 우선 test split `13193` records부터 shard 병렬화
   - shard 병렬화 사용
2. clean + embedding precompute full run
3. 최종 `frame + action + text_embedding aligned dataset` 디렉터리 구조 확정

## 중요한 메모

- subset smoke에서는 text cache time과 dataset sample time을 맞추기 위해 작은 custom setting을 사용함.
- full run에서는 raw 10 Hz를 그대로 쓰지 말고, export한 downsampled dataset을 기준으로 text cache를 만드는 쪽이 더 안전함.
