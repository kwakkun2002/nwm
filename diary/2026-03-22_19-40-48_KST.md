# 2026-03-22 19:40:48 KST

## 오늘 시작한 실제 학습

- Docker GPU visibility가 깨진 기존 `nwm_dev`를 재생성함.
- 재생성 커맨드:
  - `NWM_GPU_REQUEST='device=0' NWM_SHM_SIZE='16g' NWM_HOST_MODELS_DIR=/home/wklee/models NWM_CONTAINER_MODELS_DIR=/workspace/nwm/external_models ./scripts/docker/nwm-start.sh`
- 실제 학습 시작 커맨드:
  - `./scripts/docker/nwm-run.sh "python train.py --config config/nwm_cdit_s_recon_raw_text_dense.yaml --log-every 10 --ckpt-every 500 --eval-every 1000000"`
- 실행 중 런 산출물:
  - log: `logs/nwm_cdit_s_recon_raw_text_dense/log.txt`
  - stdout: `logs/nwm_cdit_s_recon_raw_text_dense/stdout_live_20260322_193908.log`
  - checkpoint dir: `weights/checkpoints/nwm_cdit_s_recon_raw_text_dense`
  - artifact dir: `artifacts/nwm_cdit_s_recon_raw_text_dense`

## 안정성 체크

- `step 10`: `loss 0.1396`, `0.20 steps/s`
- `step 20`: `loss 0.1496`, `2.20 steps/s`
- `step 30`: `loss 0.1231`, `2.19 steps/s`
- `step 60`: `loss 0.1300`, `2.19 steps/s`
- `step 100`: `loss 0.1274`, `2.18 steps/s`
- `step 120`: `loss 0.1286`, `2.18 steps/s`
- `step 240`: `loss 0.1326`, `2.16 steps/s`
- host `nvidia-smi` 기준 GPU 0 메모리 사용량은 약 `19.7 GiB`
- 결론:
  - `S + raw RECON + dense text cache` 조합은 실제 학습 초반 안정적으로 올라감
  - 첫 번째 blocker는 모델이 아니라 Docker shared memory였고, `NWM_SHM_SIZE='16g'`로 해결됨

## 다음 스케일 결정

- 다음 후보는 `L`이나 `XL`이 아니라 `B`
- 이유:
  - 현재 비교 기준은 `recon raw + dense text cache + checkpoint_strict=False + load_training_state=False` 조건 그대로 유지해야 함
  - 기존 `config/nwm_cdit_b.yaml`, `config/nwm_cdit_l.yaml`, `config/nwm_cdit_xl.yaml`는 멀티 데이터셋 baseline이라 바로 비교 대상이 아님
  - 동형 확장용 config로 `config/nwm_cdit_b_recon_raw_text_dense.yaml`를 추가함
- text-conditioned 파라미터 수 비교:
  - `CDiT-S/2`: `49,424,672`
  - `CDiT-B/2`: `195,678,752`
  - `CDiT-L/2`: `687,033,718`
  - `CDiT-XL/2`: `1,012,550,432`

## 논문용 최종 데이터 경로

- canonical raw data root:
  - `datasets/recon_raw/recon_release`
- canonical dense text cache root:
  - `datasets/derived/phase1_text_embeds_dense/recon_all_raw_rel`
- 관련 학습 config:
  - `config/nwm_cdit_s_recon_raw_text_dense.yaml`
  - `config/nwm_cdit_b_recon_raw_text_dense.yaml`
- 참고 산출물:
  - `datasets/derived/phase1_qwen/recon_train_1fps/all.jsonl`
  - `datasets/derived/phase1_qwen/recon_test_1fps/all.jsonl`
  - `datasets/derived/phase1_qwen_clean/recon_train_1fps_clean.jsonl`
  - `datasets/derived/phase1_qwen_clean/recon_test_1fps_clean.jsonl`
  - `datasets/derived/phase1_text_embeds/recon_train_1fps`
  - `datasets/derived/phase1_text_embeds/recon_test_1fps`
  - `datasets/derived/phase1_text_embeds_dense/recon_train_raw`
  - `datasets/derived/phase1_text_embeds_dense/recon_test_raw`
  - `datasets/derived/phase1_text_embeds_dense/recon_all_raw_rel`
- 문서 표기 원칙:
  - 컨테이너 내부 절대경로 `/workspace/nwm/...` 대신 repo-relative 경로를 canonical로 유지
